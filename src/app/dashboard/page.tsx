import {NextPage} from 'next';

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Open Page page</p>

      <h2>General Raking</h2>
    </div>
  );
};

export default Page;
