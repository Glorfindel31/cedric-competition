import {NextPage} from 'next';

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return (
    <div>
      <h1>Admin Page</h1>
      <h2>locked to one email - my</h2>
    </div>
  );
};

export default Page;
